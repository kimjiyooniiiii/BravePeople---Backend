version: 0.0
os: linux

files:
  - source:  /
    destination: /home/ubuntu/buildfiles
    overwrite: yes

permissions:
  - object: /home/ubuntu/buildfiles
    owner: ubuntu
    group: ubuntu

hooks:
  ApplicationStart:
    - location: scripts/start.sh
      timeout: 60
